tool go {
    invoke = \(inp string) -> "branch1"
}

tool echo1 {
    invoke = \(inp string) -> "echo1: "
}

tool echo2 {
    invoke = \(inp string) -> "echo2: "
}

let vars {
    chain = echo1 -> echo2
}


workflow pipeline {
    go -> vars.chain
}
