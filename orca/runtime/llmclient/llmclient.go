// A multi model proxy class that abstracts the interface between go and the llm provider using bifrost
// go native SDK and claimed to be 50x faster than litellm
package llmclient

type LLMClient interface {
}
