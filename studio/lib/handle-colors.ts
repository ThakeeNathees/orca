// Single flow edge color — every non-trigger handle (`agent`) and every
// trigger output renders as the same purple. Kept as a named export so
// consumers that may later want per-edge theming have an obvious seam.

export const AGENT_HANDLE_COLOR = "#a78bfa";
