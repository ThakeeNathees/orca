workflow wf {
    agent {
        model = model {
            provider = "gemini"
            model_name = "gemini-2.5-flash"
        }
        persona = "generate a random password"
    } -> tool {
        invoke = \(inp string) -> "password: " + inp
    }
}
