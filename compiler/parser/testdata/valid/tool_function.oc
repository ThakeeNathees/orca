// A custom function tool with inline Python invoke.
tool calculate_risk {
  desc = "Calculate risk score for a given portfolio"
  invoke = ```py
    def calculate_risk(portfolio: dict) -> float:
        return sum(portfolio.values()) * 0.1
    ```
}
