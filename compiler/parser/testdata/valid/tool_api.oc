// A tool with desc, input/output schemas, and a callable invoke.
tool slack {
  desc = "Send messages to Slack"
  input_schema = schema { channel = string message = string }
  output_schema = schema { ok = bool }
  invoke = \(msg string) -> msg
}
